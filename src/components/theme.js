import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark', // This will adapt the components for a dark background
  },
});

export default theme;